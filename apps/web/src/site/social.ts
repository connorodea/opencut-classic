// x/discord intentionally omitted -- this fork doesn't have its own
// community channels yet. Don't add real-looking placeholder URLs; consumers
// below only render those buttons when the key is present.
export const SOCIAL_LINKS: { github: string; x?: string; discord?: string } =
	{
		github: "https://github.com/connorodea/opencut-classic",
	};
